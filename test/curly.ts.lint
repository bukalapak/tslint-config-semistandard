export function test (err) {
  if (err) throw err
                    ~nil [Missing semicolon]

  if (Math.random() > 0.5)
  ~~~~~~~~~~~~~~~~~~~~~~~~
    return false
~~~~~~~~~~~~~~~~ [if statements must be braced]
                ~nil [Missing semicolon]
}
